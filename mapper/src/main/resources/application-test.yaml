# mybatis配置
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dubbo_test?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 199741zy

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: pojo